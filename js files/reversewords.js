const word = "racecar"
const word1 = "madam"
const word2 = "moonlight"
const word3 = "aviary"

function isPalindrome(word) {
    let reversed = word.split('').reverse().join('');
    if (word === reversed) {
        console.log(word + " is a palindrome.");
    } else {
        console.log(word + " is not a palindrome.");
    }
}

isPalindrome(word2)












































































































































































































h

b


v










































































































































































































































































































