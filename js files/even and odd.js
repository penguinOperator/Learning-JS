for (let i = 0; i <= 50; i++) { //set value of 0 to i. i is less than or equal to 50. add 1 to i until equal to 50
    if (i % 2 === 0) { //i divided by 2, check if even with "=== 0" and prints if true
        console.log(i); //print i
    }
}

// easier way

// let number = 6;
// let message = (number % 2 === 0) ? "Even" : "Odd";
// console.log(message); // Outputs: "Odd"
